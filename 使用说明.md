# 菜鸟包裹监控（CainiaoMonitor）

一个 Windows 桌面应用（.exe）：通过**手机号授权**监控你菜鸟裹裹/淘宝账号下的所有包裹物流。

界面采用 **Surfboard 风格**（深色玻璃拟态）设计：
- 深海军蓝渐变背景 + 青/蓝/紫柔光光斑（Surfboard 标志性视觉）；
- 半透明白玻璃卡片（大圆角 + 高光边框 + 悬浮阴影）、青 (#00D4FF) → 蓝 (#3E7BFA) 渐变强调色；
- TopBar / 底部导航 / Filled·Tonal·Outlined·Text 按钮 / Extended FAB / Chip / Snackbar；
- 卡片网格展示物流信息（清晰、克制、无广告），参考三星生活助手的快递卡片信息层级。

## 功能特性

- **数据服务商（登录页四选一）**
  1. **菜鸟账号**：手机号 + 短信验证码授权（内嵌浏览器，懒加载），Cookie 自动保存、免重复登录；
  2. **快递鸟 API**：填写 EBusinessID + AppKey，按运单号查询全部主流快递（api.kdniao.com 即时查询）；
  3. **快递100 API**：填写 customer + key（企业轮询接口）；无 key 时可用免费公开查询模式；
  4. **模拟模式**：无需任何账号，演示完整流程（列表/详情/地图/刷新/通知）。
  - 密钥与 Cookie 仅保存在本机（%APPDATA%\CainiaoMonitor）。
- **包裹列表**：自动拉取账号下所有未签收包裹，卡片网格展示（快递公司徽章、运单号、最新状态、更新时间），随窗口宽度自适应列数；顶部显示"进行中 N 件"。
- **包裹详情**：点击卡片弹出详情 —— 完整物流时间线（倒序，最新在顶部）+ **OpenStreetMap 轨迹地图**（折线连接各节点，绿=起点、红=终点，可缩放拖拽）。
- **自动刷新**：默认每 5 分钟后台刷新；有新物流动态时在**系统托盘弹出通知**；手动可点"刷新"立即更新。
- **健壮性**：登录态过期自动提示重新登录；网络异常显示友好提示并使用上次缓存数据；程序不崩溃；日志落盘便于排查。

## 数据来源说明（重要）

| 数据源 | 用途 | 说明 |
|---|---|---|
| 淘宝物流助手（wuliu.taobao.com） | 账号包裹列表 + 详情 | 菜鸟裹裹网页版旧接口已下线，这是目前**唯一无需逆向签名**的账号级网页数据源。个人使用，接口随时可能调整；页面改版导致解析失败时，原始页面会自动保存到数据目录 `debug` 文件夹 |
| 快递鸟 API（api.kdniao.com） | 按运单号查询 | 登录页选「快递鸟 API」，填 EBusinessID + AppKey（即时查询 1002） |
| 快递100（poll.kuaidi100.com / 公开查询） | 按运单号查询 | 登录页选「快递100 API」，填 customer + key（企业轮询）；无 key 用免费公开查询（部分快递需手机尾号） |
| OpenStreetMap + Nominatim | 地图与节点定位 | 开源免费；地址解析带本地缓存，遵守 1 秒 1 次限速 |

> 若淘宝物流助手的页面结构发生变化导致解析失败，请把数据目录 debug 文件夹里的 HTML 发给我，我会更新解析器。

> **API 服务商使用方法**：进入主界面后，用「＋ 添加运单」输入运单号并选择快递公司，程序即按所选服务商定时刷新；运单列表保存在本机，重启后保留。

## 目录结构

```
cainiao-monitor/
├── main.py                     # 程序入口
├── requirements.txt            # 运行依赖
├── requirements-dev.txt        # 打包依赖
├── build_full.bat              # 一键打包完整版【目录版】（推荐，运行零解压）
├── build_lite.bat              # 一键打包精简版【目录版】
├── build_full_onefile.bat      # 可选：完整版单文件打包
├── build_lite_onefile.bat      # 可选：精简版单文件打包
├── cainiao_monitor_full_onedir.spec  # 完整版目录版配置（默认）
├── cainiao_monitor_lite_onedir.spec  # 精简版目录版配置（默认）
├── cainiao_monitor_full.spec   # 完整版单文件配置（可选）
├── cainiao_monitor_lite.spec   # 精简版单文件配置（可选）
├── README.md                   # 本说明
├── tools/
│   └── gen_icon.py             # 生成应用图标 icon.ico
├── resources/                  # 图标输出目录
├── app/
│   ├── __init__.py
│   ├── config.py               # 配置管理（Cookie/设置/缓存，JSON）
│   ├── constants.py            # 快递公司表、配色、接口常量
│   ├── models.py               # 数据模型（包裹/轨迹节点）
│   ├── logger.py               # 日志
│   ├── api/
│   │   ├── http_client.py      # 统一 Session/超时/重试/异常
│   │   ├── wuliu.py            # 淘宝物流助手：列表/详情抓取与容错解析
│   │   ├── kdniao.py           # 快递鸟 API 查询
│   │   └── kuaidi100.py        # 快递100 查询
│   ├── core/
│   │   ├── monitor.py          # 后台监控线程（轮询+变化检测）
│   │   └── notifier.py         # 系统托盘与通知
│   └── ui/
│       ├── theme.py            # 主题令牌系统（深浅色+配色方案）
│       ├── icons.py            # 程序化图标（托盘/徽章/状态点）
│       ├── login_window.py     # 登录窗口（内嵌浏览器/Cookie）
│       ├── main_window.py      # 主窗口（卡片网格+托盘+设置抽屉）
│       ├── package_card.py     # 包裹卡片控件
│       ├── detail_dialog.py    # 详情弹窗（时间线）
│       ├── add_package_dialog.py # 手动添加运单
│       └── staggered_layout.py # 砖块式错位布局
└── tests/
    ├── test_core.py            # 核心逻辑测试（可离线）
    └── test_ui_smoke.py        # UI 冒烟测试（离屏截图）
```

## 一、直接运行（开发模式）

需要 Python 3.10+（推荐 3.11/3.12，打包机建议 3.11 稳定版）：

```bat
pip install -r requirements.txt
python main.py
```

## 二、打包成独立 .exe

**默认打包为「目录版」（onedir）**：产物是一个文件夹，里面的 exe 双击即用，
无需安装 Python。目录版**运行零解压**，因此不会出现单文件版常见的
`Failed to extract MSVCP140.dll: Permission denied` 报错。

1. 安装打包依赖：

```bat
pip install -r requirements-dev.txt
```

2. 一键打包（推荐，双击运行 `build_full.bat`），或手动执行：

```bat
python tools\gen_icon.py
pyinstaller --clean --noconfirm --distpath dist_full --workpath build_full cainiao_monitor_full_onedir.spec
```

3. 产物：`dist_full\CainiaoMonitor\` 目录，内含：
   - `CainiaoMonitor.exe`（主程序）
   - `启动菜鸟监控.cmd`（**本机推荐启动方式**：explorer 启动，绕开会话/任务计划程序限制）
   - `启动菜鸟监控-模拟模式.cmd`（模拟模式，无需账号）
   - 在正常 Windows 桌面上也可直接双击 `CainiaoMonitor.exe`。

> **精简版**（无内嵌浏览器，体积更小）：`build_lite.bat` 或
> `pyinstaller --clean --noconfirm cainiao_monitor_lite_onedir.spec`，
> 登录方式变为"系统浏览器 + 粘贴 Cookie"。
>
> **关于 `MSVCP140.dll Permission denied` 报错**：这是**单文件模式**启动时
> 把运行库解压到系统临时目录（%TEMP%\_MEIxxx）被拦截导致的（常见原因：
> 杀毒软件实时防护、临时目录权限限制、企业安全策略）。目录版没有解压过程，
> 不会触发该问题。若仍想用单文件版（`build_full_onefile.bat`），可尝试：
> ① 将 exe 加入杀软白名单；② 确认 %TEMP% 目录可写；③ 关闭杀软实时防护后重试。
>
> **杀毒软件误报**：PyInstaller 产物偶发被杀软误报，添加信任即可；打包已关闭 UPX 降低误报率。
>
> **打包失败排查**：PyInstaller 需要与 Python 版本匹配（≥6.16 支持 Python 3.14）；WebEngine 打包慢且占内存，失败时优先尝试精简版。

## 三、首次登录（三种方式任选）

### 方式 A：手机号授权登录（推荐）
1. 启动后填写**菜鸟/淘宝绑定手机号**；
2. 在窗口内网页中选择**短信验证码登录**（输入手机号 → 获取验证码 → 登录），也可用淘宝 App 扫码；
3. 登录成功自动检测并进入主界面，授权信息（Cookie）保存在本机，下次启动免登录。

### 方式 B：系统浏览器 + 粘贴 Cookie
1. 用 Chrome/Edge 打开 https://wuliu.taobao.com ，完成登录（短信验证码或扫码均可）；
2. 按 `F12` 打开开发者工具 → `Console` 控制台；
3. 输入 `document.cookie` 回车，复制输出；
4. 在应用「粘贴 Cookie 登录」页粘贴，填写绑定手机号（选填，用于快递100查询手机尾号），点「校验并登录」。

### 方式 B2：模拟模式体验（无需手机号）
登录窗口左下角「模拟模式体验（无需手机号）」按钮（或启动参数 `--simulate`）：
模拟「手机号 + 短信验证码」授权流程并生成演示包裹数据，**不连接任何服务器**，
可完整体验：登录 → 卡片列表 → 时间线/地图详情 → 5 分钟刷新 → 托盘通知。
适合先熟悉界面与功能；模拟模式不会写入真实授权信息。

### 方式 C：手动添加运单（不登录也能用）
主界面右上角「＋ 添加运单」→ 输入运单号、选择快递公司、填写手机尾号（部分快递需要）→ 查询并添加。此后程序会通过快递100定时刷新这些运单。

## 四、常见问题

- **登录态过期**：程序检测到后会自动弹窗提示，重新登录即可；Cookie 失效期间不崩溃、不清空已有数据。
- **内嵌浏览器白屏/无法加载**（常见于虚拟机、远程桌面、受限企业环境）：启动时加 `--no-sandbox` 参数（或设置环境变量 `CAINIAO_NO_SANDBOX=1`）再试；仍不行就用「粘贴 Cookie 登录」页。
- **提示"未能从页面解析出包裹"**：说明淘宝物流助手页面改版，原始 HTML 已保存到数据目录（菜单 → 打开日志文件位置）的 `debug` 文件夹，可发给我适配。
- **地图不显示轨迹**：节点位置通过 Nominatim 解析，若网络受限会显示"未能解析节点位置"，点击「刷新详情」重试；地图瓦片缓存在本机，之后会更快。
- **关闭窗口后程序还在**：这是托盘常驻设计，右击托盘图标 → 退出；首次最小化会有提示。
- **数据存放位置**：`%APPDATA%\CainiaoMonitor\`（config.json、日志、地图瓦片、WebEngine 缓存）。

## 四·五、免打扰

设置页（底部导航「设置」）可开启**免打扰**：不弹出系统通知（仍会正常刷新与记录日志）。

## 五、隐私与合规

- 所有登录信息（Cookie、手机号、API 密钥）仅保存在本机，不上传任何服务器；
- 数据来自第三方公开网页接口，仅供个人学习与生活使用，请勿用于商业用途；
- OpenStreetMap 瓦片与 Nominatim 服务遵守其使用条款（限速、标注来源）。

### 🔒 分享给朋友前，如何删除我的信息

**方式一（推荐）**：打开软件 → 底部导航「设置」→「清除我的信息（分享前务必执行）」→ 确认。会删除：API 密钥、Cookie、手机号、手动运单。

**方式二（手动）**：删除以下目录/文件即可（软件未运行时操作）：

```
%APPDATA%\CainiaoMonitor\config.json      ← 全部配置与密钥（最重要！）
%APPDATA%\CainiaoMonitor\webprofile\      ← 内嵌浏览器登录缓存（含登录状态）
%APPDATA%\CainiaoMonitor\tiles\          ← 地图瓦片缓存（无隐私，可留可删）
%APPDATA%\CainiaoMonitor\cainiao.log      ← 日志（含运单号等，建议删除）
```

删除后软件恢复为全新状态，可放心分享 exe 或整个 dist 目录。

## 六、免责声明

本项目为个人工具，不隶属于阿里巴巴/菜鸟/快递100 等任何公司。接口与页面可能随时调整，程序会尽力兼容；因使用本工具产生的任何影响由使用者自行承担。
